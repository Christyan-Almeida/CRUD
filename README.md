# 🛠️ CRUD Completo com API Swagger

Este projeto demonstra a implementação de um sistema **CRUD (Create, Read, Update, Delete)** consumindo uma **API REST documentada com Swagger**.  
O objetivo é fornecer um exemplo de arquitetura limpa, organizado e facilmente escalável para estudos ou aplicações reais.

---

## 🚀 Tecnologias Utilizadas

### **Frontend (caso exista)**
- React / React Native / Vue / Angular *(personalizar conforme seu projeto)*
- Axios ou Fetch API
- React Router / Expo Router (opcional)
- HTML / CSS / JS

### **Backend (API Swagger)**
- Node.js / Java / .NET / Python *(personalizar conforme necessário)*
- Swagger UI
- OpenAPI 3.0
- Banco (MySQL / SQL Server / MongoDB)

---

## 📌 Funcionalidades CRUD

O sistema permite:

| Função | Descrição |
|-------|-----------|
| **Create** | Criar novos registros via POST |
| **Read** | Listar todos os registros e buscar por ID |
| **Update** | Atualizar registros existentes |
| **Delete** | Excluir registros |

---

## 🔗 Endpoints da API

*(Exemplo — personalize com seus próprios endpoints)*

| Método | Endpoint | Descrição |
|-------|----------|-----------|
| GET    | `/api/items`           | Lista todos os itens |
| GET    | `/api/items/{id}`      | Retorna item por ID |
| POST   | `/api/items`           | Cria novo item |
| PUT    | `/api/items/{id}`      | Atualiza item existente |
| DELETE | `/api/items/{id}`      | Remove item |


Link do site: https://crud-phi-gray.vercel.app

Documenta
